services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=production
      - SESSION_SECRET=${SESSION_SECRET}
      - PORT=4200
    volumes:
      - ./data:/app/data
    restart: unless-stopped